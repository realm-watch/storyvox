<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".StoryvoxApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.NoActionBar"
        tools:targetApi="35">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboard|keyboardHidden|navigation|uiMode">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Open-with: royalroad.com/fiction/{id} -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:host="www.royalroad.com" android:pathPrefix="/fiction/" />
                <data android:host="royalroad.com" android:pathPrefix="/fiction/" />
            </intent-filter>

            <!--
                Issue #472 — Magic-link audiobook. Accept ACTION_SEND
                with text/plain so the user can share any URL from any
                app (browser, RSS reader, podcast app, social-media
                share menu) directly into storyvox. The DeepLinkResolver
                in :app inspects Intent.EXTRA_TEXT and routes to the
                Library tab with the Magic-add sheet pre-populated;
                the resolver itself handles any HTTP(S) URL via the
                Readability catch-all (never a dead-end).
            -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- Disable default WorkManager init so Hilt's Configuration.Provider takes over. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!--
            Issue #117 — FileProvider for EPUB exports. Grants ACTION_SEND
            recipients (and SAF ACTION_CREATE_DOCUMENT target apps) read
            access to files we write under cacheDir/exports/. The authority
            string is referenced from ExportFictionToEpubUseCase via
            "${context.packageName}.fileprovider" — keep in sync.

            grantUriPermissions=true is required for ACTION_SEND to flag
            FLAG_GRANT_READ_URI_PERMISSION on every chooser target. The
            cache-path entry in xml/file_paths.xml scopes the share down to
            the exports/ subdir; we don't expose the entire cacheDir.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
